package org.example.ivoprojekt.controller.utill;

import javafx.scene.control.Alert;
import org.example.ivoprojekt.api.warning.WarningAlert;

import java.io.IOException;

/*public class IOExceptionHandler extends IOException {
    public IOExceptionHandler(String errorMessage) {
        super(errorMessage);
        WarningAlert.warningAlert(Alert.AlertType.ERROR, "Chyba pri rendrovani tlacidiel!", errorMessage);
    }
}*/
